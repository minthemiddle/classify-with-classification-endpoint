# Classify message with classification endpoint

Pass messages in a CSV.  
Classify with an external classification API.  

**Usage**  
`pip install click pandas`  
`python3 classify-with-classification-endpoint.py FILE.csv`
